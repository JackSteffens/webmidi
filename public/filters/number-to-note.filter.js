'use strict';
angular.module('WebMIDI').filter('numberToNote', function () {
    return function numberToNote(number) {
        var note;
        var notes = {

        };
        return note;
    }
});