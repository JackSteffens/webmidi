import { AbstractKeyboardDesign } from './abstract-keyboard-design';

describe('AbstractKeyboardDesign', () => {
  it('should create an instance', () => {
    expect(new AbstractKeyboardDesign()).toBeTruthy();
  });
});
